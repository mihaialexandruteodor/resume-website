source "https://rubygems.org"

# Replace the default 'jekyll' gem with the official GitHub Pages gem
gem "github-pages", group: :jekyll_plugins

# # Use the 'minima' theme (GitHub Pages default)
gem "minima", "~> 2.5" 

# # Other development dependencies (optional)
group :development do
  gem "jekyll-feed", "~> 0.12"
end
